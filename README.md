# Own webpages

### Installation

You need Gulp installed globally:

```sh
$ npm i -g gulp
```
After Gulp installation run:
```sh
$ git clone [git-repo-url] awesome-webpages
$ cd awesome-webpages
$ npm update
$ gulp watch
```
